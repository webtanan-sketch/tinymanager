name: TinyManager Release

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    if: github.event_name == 'workflow_dispatch' || startsWith(github.event.head_commit.message, 'release:')
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Use npm 11.6
        shell: pwsh
        run: npm install --global npm@11.6.0

      - name: Install dependencies
        shell: pwsh
        run: npm install

      - name: Test
        shell: pwsh
        run: npm run test

      - name: Build verified PWA
        shell: pwsh
        run: npm run build

      - name: Validate PowerShell scripts
        shell: pwsh
        run: |
          $files = @(
            'packaging/windows/Start-TinyManager.ps1',
            'scripts/bootstrap-source.ps1',
            'scripts/build-release-assets.ps1'
          )
          foreach ($file in $files) {
            [void][ScriptBlock]::Create((Get-Content $file -Raw))
            Write-Host "PowerShell syntax OK: $file"
          }

      - name: Smoke test Windows localhost launcher
        shell: pwsh
        run: |
          $portable = Join-Path $PWD '.smoke-portable'
          New-Item -ItemType Directory -Force -Path (Join-Path $portable 'app') | Out-Null
          Copy-Item -Path 'dist/*' -Destination (Join-Path $portable 'app') -Recurse -Force
          Copy-Item 'packaging/windows/Start-TinyManager.ps1' $portable

          $job = Start-Job -ScriptBlock {
            param($root)
            & (Join-Path $root 'Start-TinyManager.ps1') -PreferredPort 47831 -NoBrowser
          } -ArgumentList $portable

          try {
            $ready = $false
            for ($i = 0; $i -lt 24; $i++) {
              Start-Sleep -Milliseconds 250
              try {
                $response = Invoke-WebRequest 'http://127.0.0.1:47831/' -UseBasicParsing -TimeoutSec 1
                if ($response.StatusCode -eq 200 -and $response.Content -match 'TinyManager') {
                  $ready = $true
                  break
                }
              } catch {}
            }
            if (-not $ready) {
              Receive-Job $job -Keep | Write-Host
              throw 'TinyManager localhost launcher smoke test failed.'
            }
            Write-Host 'Windows localhost launcher smoke test passed.'
          } finally {
            Stop-Job $job -ErrorAction SilentlyContinue
            Remove-Job $job -Force -ErrorAction SilentlyContinue
          }

      - name: Install Inno Setup
        shell: pwsh
        run: choco install innosetup --yes --no-progress

      - name: Build release assets
        id: assets
        shell: pwsh
        run: |
          ./scripts/build-release-assets.ps1 -OutputDirectory (Join-Path $PWD 'release') | Write-Host
          $manifest = Get-Content 'release/release-manifest.json' -Raw | ConvertFrom-Json
          "version=$($manifest.version)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          "tag=$($manifest.tag)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          "setup=$($manifest.setup)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          "portable=$($manifest.portable)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          "web=$($manifest.web)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          "source_ps=$($manifest.sourcePs)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          "source_sh=$($manifest.sourceSh)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          "registry=$($manifest.registry)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          "checksums=$($manifest.checksums)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      - name: Create or update GitHub Release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.assets.outputs.tag }}
          VERSION: ${{ steps.assets.outputs.version }}
          SETUP: ${{ steps.assets.outputs.setup }}
          PORTABLE: ${{ steps.assets.outputs.portable }}
          WEB: ${{ steps.assets.outputs.web }}
          SOURCE_PS: ${{ steps.assets.outputs.source_ps }}
          SOURCE_SH: ${{ steps.assets.outputs.source_sh }}
          REGISTRY: ${{ steps.assets.outputs.registry }}
          CHECKSUMS: ${{ steps.assets.outputs.checksums }}
        run: |
          $notes = @"
          ## TinyManager $env:TAG

          First public Alpha release with all nine modules integrated into TinyManager Core, Tiny AI/TLE, Shared People/Projects, live manager dashboard, backup/restore, and verified PWA offline support.

          ### Easiest Windows download
          Download **TinyManager-Setup-$env:TAG.exe** and run it. Node.js and Python are not required.

          ### Portable Windows
          Download **TinyManager-Windows-Portable-$env:TAG.zip**, extract it, then run `Start-TinyManager.cmd`.

          ### Web / self-hosted PWA
          Download **TinyManager-Web-$env:TAG.zip** and serve its contents over HTTPS or localhost.

          ### Developers / source workspace
          The source installers read the canonical repository registry, discover Core, all nine module repositories, and the shared Jalali engine, download the pinned revisions, and build Core automatically.

          This is an Alpha release. Keep backups of important data.
          "@
          $notesPath = Join-Path $env:RUNNER_TEMP 'release-notes.md'
          $notes | Set-Content -Path $notesPath -Encoding UTF8

          $assets = @(
            $env:SETUP,
            $env:PORTABLE,
            $env:WEB,
            $env:SOURCE_PS,
            $env:SOURCE_SH,
            $env:REGISTRY,
            $env:CHECKSUMS
          )

          gh release view $env:TAG *> $null
          if ($LASTEXITCODE -eq 0) {
            gh release upload $env:TAG @assets --clobber
          } else {
            gh release create $env:TAG @assets --target $env:GITHUB_SHA --title "TinyManager $env:TAG" --prerelease --notes-file $notesPath
          }
